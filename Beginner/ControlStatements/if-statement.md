Q) What is an if statement?

Ans) 
    An if statement is a conditional statement used to execute a block of code only when a given condition is true.
    It helps in decision-making in a program.

        💻 Code Examples
            🔹 Basic if Statement
                let age = 20;

                if (age >= 18) {
                console.log("You are eligible to vote");
                }

            🔹 if with Boolean Expression
                let isLoggedIn = true;

                if (isLoggedIn) {
                console.log("Welcome back");
                }

            🔹 if with Comparison Operator
                let score = 75;

                if (score > 50) {
                console.log("Pass");
                }

        🔍 Explanation 

        Condition inside if must evaluate to true
        If condition is false, the code block is skipped
        Used for simple decision-making