Q) What are parameters and arguments?

Ans)
    Parameters are the variables listed in a function definition, while arguments are the actual values passed to the function when it is called.

        💻 Code Example
                function add(a, b) {   // a, b → parameters
                return a + b;
                }

                add(5, 3);             // 5, 3 → arguments

        🔍 Explanation

                a and b are parameters (placeholders)
                5 and 3 are arguments (actual values)
                Parameters receive values from arguments during function call

        🔹 Another Example
                function greet(name) {   // name → parameter
                console.log("Hello " + name);
                }

                greet("Nisha");          // "Nisha" → argument

        🎯 One-Line Interview Answer

            Parameters are variables in the function definition, and arguments are the values passed to the function when it is called.