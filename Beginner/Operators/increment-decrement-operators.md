Q) What is the increment and decrement operator?

Ans) 
    The increment operator (++) increases a value by 1, and the decrement operator (--) decreases a value by 1.
    They are commonly used in loops and counter-based logic.

        💻 Code Examples
        🔹 Increment Operator (++)
                let count = 5;

                count++;
                console.log(count); // 6

        🔹 Decrement Operator (--)
                let count = 5;

                count--;
                console.log(count); // 4

        🔹 Pre vs Post Increment
                let x = 5;

                console.log(++x); // 6 (pre-increment)
                console.log(x);   // 6

                let y = 5;

                console.log(y++); // 5 (post-increment)
                console.log(y);   // 6

        🔍 Explanation 

        ++ adds 1 to a value

        -- subtracts 1 from a value

        Pre-increment (++x) updates value before use

        Post-increment (x++) updates value after use