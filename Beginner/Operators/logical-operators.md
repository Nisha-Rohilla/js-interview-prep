Q) What are logical operators?

Ans) 
    Logical operators are used to combine or manipulate Boolean values and return a Boolean result (true or false).
    They are mainly used in conditional statements and decision-making logic.

        💻 Code Examples
                let a = true;
                let b = false;

                console.log(a && b); // false  (AND)
                console.log(a || b); // true   (OR)
                console.log(!a);     // false  (NOT)

                🔹 Logical Operators in Real Use
                        let age = 22;
                        let hasID = true;

                        if (age >= 18 && hasID) {
                        console.log("Eligible");
                        }

                Logical operators are used to combine multiple conditions and return true or false in JavaScript.

                && → AND

                || → OR

                ! → NOT